import cv2

def reverse_video(input_video_path, output_video_path):

    cap = cv2.VideoCapture(input_video_path)

    frame_width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
    frame_height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
    fps = int(cap.get(cv2.CAP_PROP_FPS))

    fourcc = cv2.VideoWriter_fourcc(*'mp4v')
    out = cv2.VideoWriter(output_video_path, fourcc, fps, (frame_width, frame_height))

    frames = []

    # Read frames
    while True:
        ret, frame = cap.read()
        if not ret:
            break
        frames.append(frame)

    cap.release()

    # Reverse frames
    frames.reverse()

    # Show and save reversed frames
    for frame in frames:
        cv2.imshow("Reversed Video", frame)
        out.write(frame)

        if cv2.waitKey(25) & 0xFF == ord('q'):
            break

    out.release()
    cv2.destroyAllWindows()

    print("Reversed video saved successfully")

# Your video path
reverse_video(
    r"C:\Users\nagin\OneDrive\Desktop\data\exp 6 video.mp4",
    r"C:\Users\nagin\OneDrive\Desktop\data\reversed_video.mp4"
)
